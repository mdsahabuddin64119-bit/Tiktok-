# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unlock Exclusive Content</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        }
        body {
            background-color: #0f172a;
            color: #f8fafc;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            background: #1e293b;
            border-radius: 16px;
            padding: 32px 24px;
            max-width: 450px;
            width: 100%;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3);
            text-align: center;
            border: 1px solid #334155;
        }
        .badge {
            background: #3b82f6;
            color: #ffffff;
            font-size: 12px;
            font-weight: 700;
            padding: 4px 12px;
            border-radius: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
            display: inline-block;
            margin-bottom: 16px;
        }
        h1 {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 12px;
            color: #ffffff;
        }
        p {
            font-size: 15px;
            color: #94a3b8;
            line-height: 1.5;
            margin-bottom: 24px;
        }
        .steps-box {
            background: #0f172a;
            border-radius: 8px;
            padding: 16px;
            margin-bottom: 24px;
            text-align: left;
        }
        .step {
            font-size: 14px;
            color: #cbd5e1;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
        }
        .step:last-child {
            margin-bottom: 0;
        }
        .step-num {
            background: #334155;
            color: #60a5fa;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: bold;
            margin-right: 10px;
            flex-shrink: 0;
        }
        .cta-btn {
            display: block;
            width: 100%;
            background: #2563eb;
            color: #ffffff;
            text-decoration: none;
            padding: 16px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            transition: background 0.2s ease;
            border: none;
            cursor: pointer;
        }
        .cta-btn:hover {
            background: #1d4ed8;
        }
        .footer-text {
            font-size: 12px;
            color: #64748b;
            margin-top: 16px;
        }
    </style>
</head>
<body>

<div class="container">
    <span class="badge">Limited Access</span>
    <!-- CUSTOMIZE YOUR TITLE -->
    <h1>Complete Verification to Proceed</h1>
    <!-- CUSTOMIZE YOUR SUBTITLE -->
    <p>To access your requested file/download, please complete one quick offer from our official sponsors.</p>
    
    <div class="steps-box">
        <div class="step">
            <span class="step-num">1</span> Click the button below.
        </div>
        <div class="step">
            <span class="step-num">2</span> Complete a brief task or offer.
        </div>
        <div class="step">
            <span class="step-num">3</span> Your content will automatically unlock.
        </div>
    </div>

    <!-- REPLACE # WITH YOUR ADBLUEMEDIA OFFER / LOCKER LINK -->
    <a href="YOUR_ADBLUEMEDIA_LINK_HERE" class="cta-btn">Unlock Access Now</a>

    <p class="footer-text">Protected by anti-bot verification • Takes 1–2 minutes</p>
</div>

</body>
</html>
